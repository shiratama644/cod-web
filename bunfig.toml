# Termux Proot-Distro で .bun isolated store の長いパスが
# vitest の worker 解決に失敗するため、hoisted linker を試行
# 2026-09-23: forks.js / vmForks.js MODULE_NOT_FOUND
[install]
linker = "hoisted"

# 必要に応じてキャッシュ設定
# cache = { disableManifest = false }
