# .tmp — 検証・試作置き場

本ディレクトリは、CodWeb 開発の**検証・試作**を置く場所です。
`git` に追跡されます（`.gitignore` で除外していません）。

> 運用方針: ソース・メモ・スクリプトは追跡し、**生成物（バイナリ等）は追跡しない**。
> 各サブディレクトリの `.gitignore` で生成物のみ除外します。

## 現在の内容

| パス | 内容 |
| :-- | :-- |
| `wt_udp/` | C++ 権威ゲームサーバーの UDP 実証（プレイヤーデータ送受信）。詳細は `wt_udp/README.md`。 |
